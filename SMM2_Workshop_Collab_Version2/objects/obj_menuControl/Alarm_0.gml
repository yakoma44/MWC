/// @description HANDLES RESOLUTION CHANGES ONE FRAME AFTER 
// (RUN THIS WHENEVER DISPLAY PROPERTIES ARE CHANGED BY INVOKING alarm[0] = 1;

display_set_gui_size(ideal_width, ideal_height);
window_center();
